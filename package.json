{
  "name": "blast-radius",
  "version": "1.0.0",
  "description": "Calcula que servicios de un monorepo se ven afectados por un cambio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rollup --config rollup.config.js",
    "package": "npm run build"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^28.0.0",
    "@rollup/plugin-json": "^6.1.0",
    "@rollup/plugin-node-resolve": "^16.0.0",
    "rollup": "^4.24.0"
  }
}